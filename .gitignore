# Private strategic analysis — must never be committed or deployed.
# There is no vercel.json, so anything committed here is served as a static
# file from buyrworld.com. This audit contains the security severity register,
# commercial analysis and employment-conflict assessment.
September 2026 Progress/
*.pdf

# Local and editor noise
.DS_Store
Thumbs.db
.vercel/

# The v4 implementation pack, kept in the repository for reference while it is
# being implemented. Same reason as above: this repository is public and Vercel
# serves committed files statically, and the pack carries competitor research,
# the original audit brief and the approved concept image. START-HERE.md asks
# for it in design-handoff/, which is where it is — untracked.
design-handoff/
